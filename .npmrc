# pnpm configuration
# https://pnpm.io/npmrc

# Allow native build scripts for these packages
onlyBuiltDependencies=@clerk/shared,esbuild,sharp,workerd

# Use symlinks for better compatibility
node-linker=hoisted

# Strict peer dependencies (recommended)
strict-peer-dependencies=false

# Auto-install peer dependencies
auto-install-peers=true
